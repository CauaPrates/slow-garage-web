/**
 * PLACEHOLDER — este arquivo é gerado por `npm run types` a partir do
 * schema real do projeto Supabase. Ele está com esse conteúdo vazio
 * porque as credenciais (`SUPABASE_PROJECT_ID`) ainda não chegaram —
 * ver specs/000-foundation/spec.md, seção 11 (Dependências e riscos).
 * Assim que chegarem, rode `npm run types` e este arquivo é sobrescrito
 * com o tipo real — não edite este arquivo à mão além deste placeholder.
 */
export type Database = {
  public: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
    Enums: Record<string, never>;
    CompositeTypes: Record<string, never>;
  };
};
